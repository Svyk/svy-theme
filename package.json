{
  "name": "svy-theme",
  "version": "0.2.1",
  "private": true,
  "description": "Svy Theme — a personal, performance-first Roam Research theme (forked from rcvd/blueprint)",
  "type": "module",
  "main": "extension.js",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "scan:secrets": "node scripts/scan-secrets.mjs",
    "test": "node --test test/*.test.js",
    "verify:generated": "node scripts/verify-generated.mjs",
    "check": "npm run build && npm run scan:secrets && node --check extension.js && npm test && npm run verify:generated"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  },
  "license": "MIT"
}
