Full pre-absorption backups of both graphs' roam/css pages (block trees with uids,
dumped live via CDP 2026-08-07). These are the restore source if any absorbed module
needs to be reverted to in-graph CSS. The `-final` dumps are post-typo-fix; the
`svy-roam-css-dump-2026-08-07.json` is the earlier capture that includes the
"Blueprint CSS patches" section scaffolding. docs/absorption/absorption-map.json maps
every uid to its disposition.
